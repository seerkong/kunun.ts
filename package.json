{
  "name": "kunun-workspace",
  "version": "1.0.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "tslib": "^2.8.1"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun run build:types && bun run dist",
    "build:types": "bun run scripts/build-types.ts",
    "dist": "cd packages/kunun && bun run dist",
    "lint": "tslint \"packages/*/lib/**/*.ts\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.31",
    "tslint": "^6.1.3",
    "typescript": "~5.4.5"
  }
}
